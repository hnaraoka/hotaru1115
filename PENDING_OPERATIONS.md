# 保留中の運用作業

## PDFインポート機能の不具合修正(実例PDFで再現待ち)

### 背景

以前のフィードバック(高野さんからのLINE共有)で指摘されていた、月次報告書PDFインポート機能の不具合のうち、実物のPDFが無いと正確な原因特定が難しいとして保留にしていた2件について、2026/7/27にユーザー本人が実際に不具合を再現できるサンプルPDF(本人の2026年6月分の月次報告書。このアプリ自身で出力したもの)を提供した。次回セッションで、このPDFを使って実際に検証・修正を行う。

### バグ1: 半角スペースを含む値でスペース以降が欠落する

`src/lib/pdfImport/parseLegacyReport.ts` の `valueRightOf()`(ラベル右側の値を拾うロジック)が、pdfjs-distがテキストを半角スペースの位置で別々の `TextItem` に分割することへの対応が不十分(候補アイテムの先頭1件しか採用していない)。

提供されたサンプルPDFの「名称」テーブル、ソフトウェア/ツール行にある **"A5:SQL Mk-2"** という項目名自体に半角スペースが含まれている。インポート時にこれが "A5:SQL" と "Mk-2" の2項目に分割されてしまう可能性が高い(要検証)。技術スタックのタグ収集ロジックにも、ラベル値と同様の「隣接アイテムのみ連結する」処理が必要になる見込み。

### バグ2: 技術スタックの「ソフトウェア/ツール」「OS/クラウド/開発環境」が「DB」に吸収される

サンプルPDFの「名称」テーブルでは、DB行の項目が "Oracle" の1件のみなのに対し、直後のソフトウェア/ツール行には12件の項目が2行(2段)にわたって並んでいる。DB行が単一行・単一項目であるのに対し、ソフトウェア/ツール行は複数行にまたがるという構造の違いが、y座標ベースの行(バンド)判定を狂わせ、本来ソフトウェア/ツール行に属する項目がDB行側に誤って取り込まれる原因になっている可能性が高い(要検証)。

### 次回セッションでやること

1. 提供されたPDFを使って実際に「PDFから読み込む」機能(`ReportForm`のPDFインポート)を実行し、上記2つの不具合が本当に発生するか、どのフィールドがどう壊れるかを具体的に確認する
2. `parseLegacyReport.ts` の行(バンド)グルーピングロジックと `valueRightOf()` を修正する(以前のセッションで設計だけ済んでいた「直前アイテムとの隙間が一定値以内なら連結する」方針を実装。プランは `/root/.claude/plans/` 配下の過去プランを参照)
3. 修正後、同じPDFで再度インポートして正しく復元されることを確認する。既存の `parseLegacyReport.test.ts` に、隣接アイテム連結の回帰テストを追加する

### 注意

- ユーザーが添付したPDFファイルは今回のセッション(コンテナ)内の一時領域(`/root/.claude/uploads/...`)にあり、**次回セッションが新しいコンテナで始まる場合は失われている可能性が高い**。次回検証時に見当たらなければ、ユーザーに再度添付してもらうこと。
- 個人情報(提出者名・参画先企業名など)を含むため、このPDF自体やその内容をそのままgitにコミットしないこと。

---

この作業が完了したら、このセクションは削除してよい。
